@page
@model RapidNewsReportWebApp.Pages.Reports.IndexModel
@{
}


      <p>Found: @Model.errorCMessage ...    


        <p>Found: @Model.errorRMessage ...    
            

            @foreach (var item in Model.Reports)
            {
                <p> This is @item.Title </p>
                <p> This is @item.Id </p>
                <p> This is @item.CreatedBy </p>
                <p> This is @item.CreatedDate </p>
                <p> This is @item.PublishedDate </p>

                <a asp-page="/Reports/Edit" asp-route-id="@item.Id" class="w-20 vtn btn-primary m-2"> + Edit Report</a>
		<button type="submit" asp-handler="delete" asp-route-id="@item.Id" class="w-20 vtn btn-primary m-2"> + Delete Report</button>
		
            }

            <hr />

            @foreach (var item in Model.Comments)
            {
                <p> This is @item.CommentText </p>
                <p> This is @item.CreatedBy </p>
                <p> This is @item.CreatedDate </p>
                <p> This is @item.Id </p>
            }

            <hr />
            
            
	<a asp-page="/Reports/New" class="w-20 vtn btn-primary m-2"> + Add New Report</a>     
	
	<h3 class="clearfix">@Model.Message</h3>	
